import { redirect } from "next/navigation";

/** Default CMS landing: Reels Library (matches sidebar + Figma prototype). */
export default function CmsManagementPage() {
  redirect("/cms-management/reels");
}
